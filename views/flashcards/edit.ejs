<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FlashDeck | <%= pageTitle %></title>
    <script
        src="https://code.jquery.com/jquery-3.6.0.min.js"
        integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4="
        crossorigin="anonymous">
    </script>
    <script defer src="/script.js"></script>
    <!-- ============= FONTS ============= -->
    <!-- ============= STYLES ============= -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta2/css/all.min.css" integrity="sha512-YWzhKL2whUzgiheMoBFwW8CKV4qpHQAEuvilg9FAn5VJUDwKZZxkJNuGM4XkWuk94WCrrwslk8yWNGmY1EduTA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/normalize.css" />
    <link rel="stylesheet" href="/skeleton.css" />
    <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <%- include('../partials/nav.ejs') %>
    <div class='container' id='editBody'>
        <h1>Edit Flashcard | <%= flashcard.title %></h1>
        <form action='/flashcards/<%= flashcard._id %>?_method=PUT' method='POST'>
            <div class='row'>
                <div class='label'>Word or Concept</div>
                <input type='text' name='title' value='<%= flashcard.title %>' />
            </div>
            <div class='row'>
                <div class='label'>Definition</div>
                <input type='text' name='definition' value='<%= flashcard.definition %>' />
            </div>
            <div class='row'>
                <div class='label'>Tags <span>(separated by a comma)</span></div>
                <input type='text' name='tags' value='<%= flashcard.tags %>' />
            </div>
            <div class='row'>
                <div class='label'>References <span>(separated by a comma)</span></div>        
                <% if (urls === 'empty') { %>
                    <textarea rows='20' name='referenceURLs' placeholder='Enter references as urls separated by a comma.'></textarea>
                <% } else { %>
                    <input type='text' name='referenceURLs' value='<%= urls %>' />
                    <!-- <% urls.forEach(url => { %>
                        <input class='referenceInput' type='text' name='referenceURLs' value='<%= url %>' />
                    <% }) %> 
                        <textarea class='referenceInput' rows='20' name='referenceURLs' placeholder='Enter references as urls separated by a comma.'></textarea> -->
                <% } %>
            </div>
            <div id='bottomRow'>
                <input type='submit' value='EDIT Flashcard' />
            </div>
        </form>
    </div>
</body>
<%- include('../partials/footer.ejs') %>