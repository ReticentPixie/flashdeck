<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FlashDeck | <%= pageTitle %></title>
    <script
        src="https://code.jquery.com/jquery-3.6.0.min.js"
        integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4="
        crossorigin="anonymous">
    </script>
    <script defer src="/script.js"></script>
    <!-- ============= FONTS ============= -->
    <!-- ============= STYLES ============= -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta2/css/all.min.css" integrity="sha512-YWzhKL2whUzgiheMoBFwW8CKV4qpHQAEuvilg9FAn5VJUDwKZZxkJNuGM4XkWuk94WCrrwslk8yWNGmY1EduTA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/normalize.css" />
    <link rel="stylesheet" href="/skeleton.css" />
    <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <%- include('../partials/nav.ejs') %>
    <div class='container' id='showBody'>
        <div id='showBodyActions'>
            <a href='/flashcards/<%= flashcard.id %>/edit'><input class="button hvr-grow" type="submit" value="EDIT"></a>
            <form action='/flashcards/<%= flashcard.id %>?_method=DELETE' method='POST'>
                <input class='button hvr-grow' type='submit' value='DELETE'>
            </form>
            <a href='/flashcards/<%= flashcard.id %>'><input class="button hvr-grow" type="submit" value="Reset Flashcard"></a>
        </div>
        <div>
            <!-- <h1><%= flashcard.title %></h1>   -->
        </div>
        <div id='flashcard'>
            <img src=https://images.freeimages.com/images/large-previews/63d/index-card-1413900.jpg alt='See the Deck'>
            <h5 id='title' style='font-weight: bolder; text-align: center; text-transform: capitalize;'><%= flashcard.title %> <br>
                <span style='font-size: smaller; font-style: italic; font-weight: lighter;'>Click to see the definition!</span></h5>
            <h5 id='definition' hidden><%= flashcard.definition %></h5>
        </div>
        <div id='columns'>
            <div id='tags'>
                <ul>Tags/Keywords:
                    <% if (tags === 'empty') { %>
                        <li>Edit card to add tags</li>
                    <% } else { %>
                        <% tags.forEach(tag => { %>
                            <li><%= tag %></li>
                        <% })
                     } %>
                </ul>
            </div>
            <div id='references'>
                <ul> References: 
                    <% if (urls === 'empty') { %>
                        <li>Edit card to add references</li>
                    <% } else { %>
                        <% urls.forEach(url => { %>
                            <li><a href='<%= url %>' target='blank'><%= url %></a></li>
                        <% })
                     } %>
                </ul>
            </div>
        </div>
    </div>
</body>
<%- include('../partials/footer.ejs') %>